<div class="alert">
  <button type="button" class="close" data-dismiss="alert">&#215;</button>
  <strong>Warning!</strong> Roles are used for authorization via the <%= link_to "CanCan", "https://github.com/ryanb/cancan" %> gem. Additions, 
	deletions, or changes in roles must also be reflected in the Ability class found in app/models/ability.rb. Do not make changes to the roles 
	without updating the code and redeploying the application. 
</div>

<h3>Roles</h3>
<table class="table table-condensed table-striped table-bordered">
  <thead>
    <tr>
      <th>Name</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  <% @roles.each do |role| %>
    <tr>
      <td><%= link_to role.name, edit_role_path(role) %></td>
      <td><%= link_to("Delete role", role_path(role), :data => { :confirm => "Are you sure?" }, :method => :delete, :class => 'btn btn-mini btn-danger') %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<%= link_to("New role", new_role_path, :class => 'btn btn-mini') %>
