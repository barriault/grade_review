<div class="well">
	<div class="page-header">
		<h3>
			<%= Term.current.name %> Appeals
			<span class="pull-right"><%= link_to "Download CSV", appeals_reports_path(format: 'csv'), :class => 'btn btn-mini btn-primary' %></span>
		</h3>
	</div>
	<%= render 'standard_report' %>
</div>
