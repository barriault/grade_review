<div class="well">
	<div class="page-header">
		<h3>
			<%= Term.current.name %> Departmental Suspensions
			<span class="pull-right"><%= link_to "Download CSV", departmental_suspensions_reports_path(format: 'csv'), :class => 'btn btn-mini btn-primary' %></span>
		</h3>
	</div>
	<%= render 'standard_report' %>
</div>
